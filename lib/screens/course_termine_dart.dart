import 'package:flutter/material.dart';

class CourseTerminePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Course Terminée'),
      ),
      body: Center(
        child: Text('Course Terminée Page en developement par mlle Ango esther '),
      ),
    );
  }
}
