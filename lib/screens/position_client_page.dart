import 'package:flutter/material.dart';

class PositionClientPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Position Client'),
      ),
      body: Center(
        child: Text('Position Client Page Content'),
      ),
    );
  }
}
